# vars file for innotop
---
innotop_dependencies:
  - "{{ 'git-core' if ansible_distribution_version != '18.04' else 'git' }}"
  - libdbd-mysql-perl
  - libterm-readkey-perl

innotop_checkout_path: /var/lib/ansible/innotop/checkouts/innotop
